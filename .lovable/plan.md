

# Home Page → Currency Strength Page

## কি করবো
Default route `/` এ `Index` page এর বদলে `CurrencyStrength` page দেখাবো।

## Change

| File | Change |
|------|--------|
| `src/App.tsx` | `"/"` route এর element `<Index />` → `<CurrencyStrength />` করবো |

1 লাইন change।

